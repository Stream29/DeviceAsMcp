# DeviceAsMcp

Kotlin Multiplatform project.

## Project status

- Base namespace: `io.github.stream29.mcp.device`
- Concrete modules and platform targets are not initialized yet.

## Build

```shell
./gradlew check
```
